name: Run unit tests with Python

on: [push]

jobs:
  - uses: actions/checkout@v2
    build:
       runs-on: ubuntu-latest

       steps:
        - name: Run script
          run: |
             ls -la 
             pwd
             python3 logic.py
             mkdir -p aaa

        - name: Check run
          run: |
              pwd
              ls -la
   test:
       needs: build
       runs-on: ubuntu-latest

       steps:
          - name: aaaaa
            run: | 
              echo "aaaaa"
              ls -la
              pwd

