name: Run unit tests with Python

on: [push]

jobs:
   build:
       runs-on: ubuntu-latest

       strategy:
            matrix:
        
                python-version: [3.7]

       steps:
        - name: Print messages
          run: |
              echo "Hello World!"
              dir
        - name: Run script
          run: |
              python3 logic.py
