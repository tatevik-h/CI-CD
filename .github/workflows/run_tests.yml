name: Run unit tests with Python

on: [push]

jobs:
   build:
       runs-on: ubuntu-latest

       strategy:
            matrix:
        
                python-version: [3.7]

       steps:
        - name: Print messages
          run: |
             mkdir workspaces && cd workspaces     
        - name: Run script
          run: |
              pwd
